# Migration `20190607164817-bypass-byoi`

This migration has been generated by Divyendu Singh at 6/7/2019, 4:48:17 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "migration_engine"."new_Group";

PRAGMA foreign_keys=OFF;

CREATE TABLE "migration_engine"."new_Group" ("id" INTEGER NOT NULL, "meetup_id" TEXT NOT NULL, "user" INTEGER REFERENCES User(id), PRIMARY KEY ("id"));

INSERT INTO new_Group (id,user) SELECT id,user from Group

DROP TABLE "migration_engine"."Group";

ALTER TABLE "migration_engine"."new_Group" RENAME TO "Group";

PRAGMA "migration_engine".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration last migration id..20190607164817-bypass-byoi
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,17 @@
+model User {
+  id: Int @id
+  email: String? @unique # Meetup doesn't provide email. We need to provide user another way to do this
+  meetup_id: String @unique # Using this to find uniques since email is not available
+  groups: Group[]
+  access_token: String?
+  refresh_token: String?
+}
+
+enum Provider {
+  MEETUP
+}
+
+model Group {
+  id: Int @id
+  meetup_id: String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190607164817-bypass-byoi)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190607164817-bypass-byoi'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
