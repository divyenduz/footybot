# Migration `20190613134558-init`

This migration has been generated by Divyendu Singh at 6/13/2019, 1:45:58 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:migration_engine"."User" ("id" INTEGER NOT NULL, "email" TEXT, "meetup_id" TEXT NOT NULL, "access_token" TEXT, "refresh_token" TEXT, PRIMARY KEY ("id"));

CREATE TABLE "file:migration_engine"."Entity" ("id" INTEGER NOT NULL, "meetup_id" TEXT NOT NULL, "user" INTEGER REFERENCES User(id), PRIMARY KEY ("id"));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190613134558-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,27 @@
+datasource db {
+  provider = "sqlite"
+  url = "file:migration_engine.db"
+}
+
+generator photon {
+  provider = "typescript"
+  output = "node_modules/@generated/photon"
+}
+
+model User {
+  id Int @id
+  email String? @unique // Meetup doesn't provide email. We need to provide user another way to do this
+  meetup_id String @unique // Using this to find uniques since email is not available
+  groups Entity[]
+  access_token String?
+  refresh_token String?
+}
+
+enum Provider {
+  MEETUP
+}
+
+model Entity {
+  id Int @id
+  meetup_id String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190613134558-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190613134558-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
