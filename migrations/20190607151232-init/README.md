# Migration `20190607151232-init`

This migration has been generated by Divyendu Singh at 6/7/2019, 3:12:32 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "migration_engine"."User" ("id" INTEGER NOT NULL, "email" TEXT, "meetup_id" TEXT NOT NULL, "access_token" TEXT, "refresh_token" TEXT, PRIMARY KEY ("id"));

CREATE TABLE "migration_engine"."Group" ("id" INTEGER NOT NULL, "user" INTEGER REFERENCES User(id), PRIMARY KEY ("id"));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration last migration id..20190607151232-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,16 @@
+model User {
+  id: Int @id
+  email: String? @unique # Meetup doesn't provide email. We need to provide user another way to do this
+  meetup_id: String @unique # Using this to find uniques since email is not available
+  groups: Group[]
+  access_token: String?
+  refresh_token: String?
+}
+
+enum Provider {
+  MEETUP
+}
+
+model Group {
+  id: Int @id # Same as meetup
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190607151232-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190607151232-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
