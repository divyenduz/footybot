# Migration `20190606193620-init`

This migration has been generated by Divyendu Singh at 6/6/2019, 7:36:20 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "migration_engine"."User" ("id" INTEGER NOT NULL, "email" TEXT, "meetup_id" TEXT NOT NULL, PRIMARY KEY ("id"));

CREATE TABLE "migration_engine"."Token" ("id" INTEGER NOT NULL, "provider" TEXT NOT NULL, "access_token" TEXT, "refresh_token" TEXT, "user" INTEGER REFERENCES User(id), PRIMARY KEY ("id"));

CREATE TABLE "migration_engine"."Group" ("id" INTEGER NOT NULL, "user" INTEGER REFERENCES User(id), PRIMARY KEY ("id"));
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration last migration id..20190606193620-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,22 @@
+model User {
+  id: Int @id
+  email: String? @unique # Meetup doesn't provide email. We need to provide user another way to do this
+  meetup_id: String @unique # Using this to find uniques since email is not available
+  groups: Group[]
+  token: Token[]
+}
+
+model Token {
+  id: Int @id
+  provider: Provider
+  access_token: String?
+  refresh_token: String?
+}
+
+enum Provider {
+  MEETUP
+}
+
+model Group {
+  id: Int @id # Same as meetup
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190606193620-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190606193620-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
